def get_indices_of_item_weights(weights, limit):
    weight_table = {}
    for i in range(len(weights)):
      weight_table[str(weights[i])] = i

if __name__ == '__main__':
  # You can write code here to test your implementation using the Python repl
  pass 